# Обрезка ссылок с помошью Битли

[TODO: Получение коротких ссылок с помощью API сервиса Битли. Порлучение количества переходов по ссылкам ]

### Как установить

[TODO: Передача аргумента в командной строке. Пример 'python main.py https://google.com'. Если не передан параметр в командной строке, передача URL осуществляется в терминале]

Python3 должен быть уже установлен. 
Затем используйте `pip` (или `pip3`, есть конфликт с Python2) для установки зависимостей:
```
pip install -r requirements.txt
```

### Цель проекта

Код написан в образовательных целях на онлайн-курсе для веб-разработчиков [dvmn.org](https://dvmn.org/).